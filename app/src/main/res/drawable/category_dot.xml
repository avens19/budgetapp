<?xml version="1.0" encoding="utf-8"?>
<!-- Tinted per category at runtime via ImageViewCompat.setImageTintList. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" />
    <size
        android:width="@dimen/category_dot"
        android:height="@dimen/category_dot" />
</shape>
