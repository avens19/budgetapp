<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <TextView
        android:id="@+id/switch_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/label_switch_budget"/>

    <ListView
        android:id="@+id/switch_list"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_below="@id/switch_label"/>

    <LinearLayout
        android:id="@+id/switch_separator"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_below="@id/switch_list"
        android:background="@color/black"/>

    <TextView
        android:id="@+id/switch_label_add"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/label_add_budget"
        android:layout_below="@id/switch_separator"
        android:layout_marginTop="10dp"/>

    <Button
        android:id="@+id/switch_add_budget"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:layout_below="@id/switch_label_add"
        android:text="@string/button_new_budget"
        android:onClick="newBudgetOnClick" />

    <Button
        android:id="@+id/switch_join_budget"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:layout_below="@id/switch_add_budget"
        android:text="@string/button_join_budget"
        android:onClick="joinBudgetOnClick" />


</RelativeLayout>