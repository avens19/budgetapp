<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/join_budget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingStart="@dimen/activity_horizontal_margin"
    android:paddingEnd="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <TextView
        android:id="@+id/label_join_unique_id"
        android:labelFor="@+id/text_join_unique_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/label_join_unique_id" />

    <EditText
        android:id="@+id/text_join_unique_id"
        android:importantForAutofill="no"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/label_join_unique_id"
        android:layout_alignEnd="@+id/label_join_unique_id"
        android:layout_below="@+id/label_join_unique_id"
        android:ems="10"
        android:inputType="textNoSuggestions|textMultiLine"
        android:lines="2" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/button_go"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text_join_unique_id"
        android:layout_centerHorizontal="true"
        android:onClick="goButtonOnClick"
        android:text="@string/button_go" />

</RelativeLayout>