<?xml version="1.0" encoding="utf-8"?>
<!-- Shared placeholder. Callers set the icon, title, body and (optionally)
     the action button, which is hidden until it is given a label. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/empty_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="@dimen/space_xxl"
    android:paddingTop="@dimen/space_xxl"
    android:paddingEnd="@dimen/space_xxl"
    android:paddingBottom="@dimen/space_xl">

    <ImageView
        android:id="@+id/empty_icon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:alpha="0.5"
        android:contentDescription="@null"
        app:tint="?attr/colorOnSurfaceVariant"
        tools:src="@drawable/ic_receipt" />

    <TextView
        android:id="@+id/empty_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_l"
        android:gravity="center"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:textColor="?attr/colorOnSurface"
        tools:text="Nothing spent yet" />

    <TextView
        android:id="@+id/empty_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_s"
        android:gravity="center"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="Expenses you add for this week will show up here." />

</LinearLayout>
